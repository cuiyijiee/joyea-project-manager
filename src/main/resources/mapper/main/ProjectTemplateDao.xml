<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.cuiyijie.joyea.dao.main.ProjectTemplateDao">


    <insert id="insert">
        INSERT INTO project_template (name, category, enabled)
        VALUES (#{item.name}, #{item.category}, #{item.enabled})
    </insert>

    <select id="findAll" resultType="me.cuiyijie.joyea.domain.ProjectTemplate">
        SELECT id AS id,
        name AS name,
        category AS category,
        enabled AS enabled
        FROM project_template
        WHERE 1=1
        <if test="item.id != null">
            AND id = #{item.id}
        </if>
        <if test="item.name != null and item.name != ''">
            AND name = #{item.name}
        </if>
        <if test="item.category != null and item.category != ''">
            AND name = #{item.category}
        </if>
        <if test="item.enabled != null">
            AND enabled = #{item.enabled}
        </if>
    </select>

    <update id="update">
        UPDATE project_template
        SET name = #{item.name}, category = #{item.category},
            enabled = #{item.enabled}
        WHERE id = #{item.id}
    </update>

    <delete id="delete">
        DELETE
        FROM project_template
        WHERE id = #{item.id}
    </delete>

</mapper>